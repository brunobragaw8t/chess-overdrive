// Droppable-target
export const TARGET_RING_CLASSES = ["border-accent", "ring-2", "ring-accent/40"] as const;
export const TARGET_RING = TARGET_RING_CLASSES.join(" ");

// Placement-target pulse indicator
export const TARGET_PULSE_CLASSES = ["border-accent", "animate-pulse", "cursor-pointer"] as const;
export const TARGET_PULSE = TARGET_PULSE_CLASSES.join(" ");
